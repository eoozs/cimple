program sum {
    declare x, sum;

    input(x);
    sum := 0;

    forcase
        case (x > 0) {
            sum := sum + x;
            x := x - 1;
        }
        default
            print(sum);
}.
